<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if @user.name == "admin" %>
    | <%= link_to "削除", user, method: :delete,
                                  data: { confirm: "削除しますか?" } %>
  <% end %>
</li>